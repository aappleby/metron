#include "metron_tools.h"

// Returns that are not at the end of a function body are (probably) bad.

class Module {
public:

  logic<8> tock() {
    logic<8> x = 1;
    return x;
    x = x + 1;
  }
};
